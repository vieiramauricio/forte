import styled from 'styled-components';

const ValidateMessage = styled.span`
  display: block;
  /* font-weight: bold; */
  color: #ff0000;
  position: absolute;
  text-align: center;
  font-size: 12px;
`;

export default ValidateMessage;
